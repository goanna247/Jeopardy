import React, { Component } from 'react';
import { Link } from 'react-router-dom';

export default class OrganiserView extends Component {
  render() {
    return(
      <nav>
        <h1>Organiser View</h1>
      </nav>
    );
  }
}